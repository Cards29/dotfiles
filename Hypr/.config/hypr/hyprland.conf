# #######################################################################################
# AUTOGENERATED HYPRLAND CONFIG.
# PLEASE USE THE CONFIG PROVIDED IN THE GIT REPO /examples/hyprland.conf AND EDIT IT,
# OR EDIT THIS ONE ACCORDING TO THE WIKI INSTRUCTIONS.
# #######################################################################################

# This is an example Hyprland config file.
# Refer to the wiki for more information.
# https://wiki.hyprland.org/Configuring/

# Please note not all available settings / options are set here.
# For a full list, see the wiki

# You can split this configuration into multiple files
# Create your files separately and then link them to this file like this:
# source = ~/.config/hypr/myColors.conf

################
### SOURCES ####
################

#source = $HOME/.config/hypr/colors.conf


################
### MONITORS ###
################

# See https://wiki.hyprland.org/Configuring/Monitors/
monitor=,preferred,auto,1.8


###################
### MY PROGRAMS ###
###################

# See https://wiki.hyprland.org/Configuring/Keywords/

# Set programs that you use
$terminal = kitty
$fileManager = nautilus
$browser = brave
$colorPicker = hyprpicker
$OCR4Linux = ~/.config/OCR4Linux/OCR4Linux.sh
$OCR4Linux_ENG = ~/.config/OCR4Linux/OCR4Linux.sh --lang eng

#################
### AUTOSTART ###
#################

# Autostart necessary processes (like notifications daemons, status bars, etc.)
# Or execute your favorite apps at launch like this:

exec-once = swaync & hypridle & swayosd-server
exec-once = swww-daemon
exec-once = hyprctl setcursor Bibata-Modern-Ice 24
exec-once = /usr/lib/hyprpolkitagent
exec-once = caelestia shell -d
exec-once = hyprpm reload -n
#to make cmatrix your background
#exec-once = kitty +kitten panel --edge=background -o font_size=12 cmatrix

############################
### ENVIRONMENT VARIABLES ###
#############################

# See https://wiki.hyprland.org/Configuring/Environment-variables/

env = HYPRSHOT_DIR, /home/arcane/Pictures/Screenshots

# these are from https://github.com/gaurav23b/simple-hyprland

# QT
env = QT_QPA_PLATFORM,wayland;xcb
env = QT_QPA_PLATFORMTHEME,qt6ct
env = QT_WAYLAND_DISABLE_WINDOWDECORATION,1
env = QT_AUTO_SCREEN_SCALE_FACTOR,1
env = QT_STYLE_OVERRIDE,kvantum

# Toolkit Backend Variables
#env = GDK_BACKEND,wayland,x11,*
#env = SDL_VIDEODRIVER,wayland
#env = CLUTTER_BACKEND,wayland

# XDG Specifications
env = XDG_CURRENT_DESKTOP,Hyprland
env = XDG_SESSION_TYPE,wayland
env = XDG_SESSION_DESKTOP,Hyprland


###################
### PERMISSIONS ###
###################

# See https://wiki.hyprland.org/Configuring/Permissions/
# Please note permission changes here require a Hyprland restart and are not applied on-the-fly
# for security reasons

# ecosystem {
#   enforce_permissions = 1
# }

# permission = /usr/(bin|local/bin)/grim, screencopy, allow
# permission = /usr/(lib|libexec|lib64)/xdg-desktop-portal-hyprland, screencopy, allow
# permission = /usr/(bin|local/bin)/hyprpm, plugin, allow




###################
##### Plugins #####
###################

plugin {
  hyprexpo {
    columns = 3
    gap_size = 5
    bg_col = rgb(111111)
    workspace_method = first 1 # [center/first] [workspace] e.g. first 1 or center m+1

    gesture_distance = 300 # how far is the "max"
  }
  hyprbars {
    # example config
    bar_height = 20

    # example buttons (R -> L)
    # hyprbars-button = color, size, on-click
    hyprbars-button = rgb(ff4040), 10, 󰖭, hyprctl dispatch killactive
    hyprbars-button = rgb(eeee11), 10, , hyprctl dispatch fullscreen 1

    # cmd to run on double click of the bar
    on_double_click = hyprctl dispatch fullscreen 1
  }
  overview {
    # ---- Layout ----
    centerAligned = true      # macOS/KDE style (centered thumbnails)
    onBottom      = false     # Panel at bottom (set false for top)
    panelHeight   = 150       # px – taller = bigger previews
  
    # ---- Behavior ----
    hideEmptyWorkspaces = false
    autoDrag      = true      # Drag window → auto-switch workspace
    dragCreatesWorkspace = true
  
    # ---- Exit / Close ----
    exitOnClickEmpty = true   # Click empty space → close
    exitKey          = Escape # "" to disable key exit
  
    # ---- Animations ----
    openAnim      = slide     # slide / fade / zoom
    closeAnim     = slide
    animDuration  = 250       # ms
  
    # ---- Visual Polish (optional) ----
    borderSize    = 2
    borderColor   = rgba(88c0d0ff)   # nord cyan
    backgroundColor = rgba(2e3440aa) # nord dark, semi-transparent
    overrideGaps  = true
  
    # ---- Rounded corners & shadows (requires hyprland ≥0.41) ----
    roundCorners  = 12
    dropShadow    = true
    shadowRange   = 20
    shadowRenderPower = 3
  }
}



#####################
### LOOK AND FEEL ###
#####################

# Refer to https://wiki.hyprland.org/Configuring/Variables/

# https://wiki.hyprland.org/Configuring/Variables/#general
general {
    gaps_in = 3 
    gaps_out = 3

    border_size = 2

    # https://wiki.hyprland.org/Configuring/Variables/#variable-types for info about colors
    col.active_border = rgba(33ccffee) rgba(00ff99ee) 45deg
    col.inactive_border = rgba(595959aa)

    # Set to true enable resizing windows by clicking and dragging on borders and gaps
    resize_on_border = false

    # Please see https://wiki.hyprland.org/Configuring/Tearing/ before you turn this on
    allow_tearing = false

	layout = dwindle
	#layout = master

}

# https://wiki.hyprland.org/Configuring/Variables/#decoration
decoration {
    rounding = 20
    rounding_power = 4

    # Change transparency of focused and unfocused windows
    active_opacity = 1.0
    inactive_opacity = 1.0

    shadow {
        enabled = true
        range = 4
        render_power = 3
        color = rgba(000000aa)
	      scale = 1.0
    }

    # https://wiki.hyprland.org/Configuring/Variables/#blur
    blur {
        enabled = true
        size = 9
        passes = 3

      	ignore_opacity = true
      	new_optimizations = true
      	
      	xray = false
      
      	noise = 0.15
      	contrast = 1.5
      	brightness = 0.8172
      
        vibrancy = 0.1696
      	vibrancy_darkness = 0.0
    }
    # Disable client-side decorations for all windows
	  # no_csd = true

}

# https://wiki.hyprland.org/Configuring/Variables/#animations
    
animations {
    enabled = yes

    # Your custom curve (used for windows and layers)
    bezier = myBezier, 0.05, 0.9, 0.1, 1.05
    # Other beziers 
    bezier = easeOutQuint,0.23,1,0.32,1
    bezier = easeInOutCubic,0.65,0.05,0.36,1
    bezier = linear,0,0,1,1
    bezier = almostLinear,0.5,0.5,0.75,1.0
    bezier = quick,0.15,0,0.1,1

    # GLOBAL (Fallback for everything)
    animation = global, 1, 4, default 

    # WINDOWS (Windows open/close and move. Sets speed/curve for windowsIn/Out)
    animation = windows, 1, 4, myBezier, slide
    
    # BORDERS (Independent border animation)
    animation = border, 1, 10, easeOutQuint # Slower border animation is fine

    # LAYERS (Popups, notifications, etc.)
    animation = layers, 1, 1.5, quick, poppin 80%
    
    # WORKSPACES (Workspace switch animation.)
    animation = workspaces, 1, 2, myBezier, slide
    
    # FADES (Window fade transitions)
    animation = fade, 1, 2, quick

    # Optional: If you want window/layer fades to be even faster:
    # animation = fadeIn, 1, 2, almostLinear
    # animation = fadeOut, 1, 2, almostLinear
}

# Ref https://wiki.hyprland.org/Configuring/Workspace-Rules/
# "Smart gaps" / "No gaps when only"
# uncomment all if you wish to use that.
# workspace = w[tv1], gapsout:0, gapsin:0
# workspace = f[1], gapsout:0, gapsin:0
# windowrule = bordersize 0, floating:0, onworkspace:w[tv1]
# windowrule = rounding 0, floating:0, onworkspace:w[tv1]
# windowrule = bordersize 0, floating:0, onworkspace:f[1]
# windowrule = rounding 0, floating:0, onworkspace:f[1]

# See https://wiki.hyprland.org/Configuring/Dwindle-Layout/ for more
dwindle {
    pseudotile = true # Master switch for pseudotiling. Enabling is bound to mainMod + P in the keybinds section below
    preserve_split = true # You probably want this
}

# See https://wiki.hyprland.org/Configuring/Master-Layout/ for more
master {
    new_status = master
}

# https://wiki.hyprland.org/Configuring/Variables/#misc
misc {
    force_default_wallpaper = 0 # Set to 0 or 1 to disable the anime mascot wallpapers
    disable_hyprland_logo = false # If true disables the random hyprland logo / anime girl background. :(
}


#############
### INPUT ###
#############

# https://wiki.hyprland.org/Configuring/Variables/#input
input {
    kb_layout = us
    kb_variant =
    kb_model =
    kb_options = 
    kb_rules =

    follow_mouse = 1

    sensitivity = 0 # -1.0 - 1.0, 0 means no modification.

    touchpad {
        natural_scroll = true
    }
}

# https://wiki.hyprland.org/Configuring/Variables/#gestures
# Syntax: gesture = <fingers>, <direction>, <action>, <options>

gesture = 4, horizontal, workspace, 
gesture = 4, up, dispatcher, hyprexpo:expo toggle


# Example per-device config
# See https://wiki.hyprland.org/Configuring/Keywords/#per-device-input-configs for more
device {
    name = epic-mouse-v1
    sensitivity = -0.5
}


###################
### KEYBINDINGS ###
###################

# See https://wiki.hyprland.org/Configuring/Keywords/
$mainMod = SUPER # Sets "Windows" key as main modifier

# Example binds, see https://wiki.hyprland.org/Configuring/Binds/ for more

# Terminal and Quit Windows
bind = $mainMod, return, exec, $terminal
bind = $mainMod SHIFT, return, exec, $terminal --title CenteredKitty
bind = $mainMod, Q, killactive,

# Exit wayland
bind = $mainMod SHIFT, Q, exit,

# Hyprpicker
bind = $mainMod, P, exec, $colorPicker -a -l

# Lockscreen, Shutdown and Reboot
bind = $mainMod SHIFT, L, exec, hyprlock
bind = $mainMod SHIFT, P, exec, shutdown now 
bind = $mainMod SHIFT, R, exec, reboot 

# Go Fullscreen
bind = $mainMod, F, fullscreen, 1
bind = $mainMod SHIFT, F, fullscreen, 0 

# App Launchers
bind = $mainMod, B, exec, $browser
bind = $mainMod, E, exec, $fileManager

# Open Rofi
bind = $mainMod, space, exec, ~/.config/rofi/scripts/launcher_t1

# Use ocrdesktop
bind = $mainMod, O, exec, $OCR4Linux # OCR4Linux with interactive selection

# Hyprland Window Utilities
#bind = $mainMod SHIFT, space, togglefloating,
bind = $mainMod, T, togglesplit, # dwindle
#bind = $mainMod, D, pseudo, # dwindle

# For reloading waybar
bind = $mainMod, W, exec, killall waybar || waybar

# move focus with mainmod + arrow keys
bind = $mainMod, H, movefocus, l
bind = $mainMod, L, movefocus, r
bind = $mainMod, K, movefocus, u
bind = $mainMod, J, movefocus, d

bind = $mainMod, left, movefocus, l
bind = $mainMod, right, movefocus, r
bind = $mainMod, up, movefocus, u
bind = $mainMod, down, movefocus, d

# For taking screenshot with hyprshot
bind = $mainMod, S, exec, hyprshot -m region
bind = $mainMod SHIFT, S, exec, hyprshot -m active -m output

## Plugins ##
# Hyprexpo
bind = $mainMod, G, hyprexpo:expo, toggle
# Hyprspace
# bind = $mainMod, O, overview:toggle

# Switch workspaces with mainMod + [0-9]
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10

# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10

# Example special workspace (scratchpad)
bind = $mainMod, M, togglespecialworkspace, magic
bind = $mainMod SHIFT, M, movetoworkspace, special:magic

# Scroll through existing workspaces with mainMod + scroll
bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow

# Laptop multimedia keys for volume and LCD brightness
bindel = ,XF86AudioRaiseVolume, exec, swayosd-client --output-volume 5
bindel = ,XF86AudioLowerVolume, exec, swayosd-client --output-volume -5
bindel = ,XF86AudioMute, exec, swayosd-client --output-volume mute-toggle
bindel = ,XF86AudioMicMute, exec, swayosd-client --input-volume mute-toggle
bindel = ,XF86MonBrightnessUp, exec, swayosd-client --brightness +5
bindel = ,XF86MonBrightnessDown, exec, swayosd-client --brightness -5

# Requires playerctl
bindl = , XF86AudioNext, exec, playerctl next
bindl = , XF86AudioPause, exec, playerctl play-pause
bindl = , XF86AudioPlay, exec, playerctl play-pause
bindl = , XF86AudioPrev, exec, playerctl previous

##############################
### WINDOWS AND WORKSPACES ###
##############################

# See https://wiki.hyprland.org/Configuring/Window-Rules/ for more
# See https://wiki.hyprland.org/Configuring/Workspace-Rules/ for workspace rules

# Example windowrule
# windowrule = float,class:^(kitty)$,title:^(kitty)$

# Ignore maximize requests from apps. You'll probably like this.
windowrule = suppressevent maximize, class:.*

# Fix some dragging issues with XWayland
windowrule = nofocus,class:^$,title:^$,xwayland:1,floating:1,fullscreen:0,pinned:0

windowrulev2 = opacity 0.9 0.9,class:code
#windowrulev2 = opacity 0.8 0.8,class:org.telegram.desktop
#windowrulev2 = opacity 0.8 0.8,class:discord

windowrulev2 = float, title:^(CenteredKitty)$
windowrulev2 = center, title:^(CenteredKitty)$
windowrulev2 = size 1040c 636c, title:^(CenteredKitty)$

#this line is to fix blurrines of apps who use xwayland,
#by rehinur

#xwayland {
#	force_zero_scaling = true
#}
