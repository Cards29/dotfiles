(defcfg
  process-unmapped-keys yes
)

(defsrc
  caps
)

(defvar 
  tap-time 150 hold-time 200
) 

(defalias
  caps_esc_ctrl (tap-hold $tap-time $hold-time esc lctrl)
)

(deflayer base
  @caps_esc_ctrl
)
